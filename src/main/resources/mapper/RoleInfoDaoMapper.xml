<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.vueshop.manager.dao.mapper.RoleInfoDao">

    <resultMap id="RoleInfoMap" type="com.vueshop.manager.dao.model.RoleInfo">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="role_name" property="roleName" jdbcType="VARCHAR"/>
        <result column="role_desc" property="roleDesc" jdbcType="VARCHAR"/>
        <result column="status" property="status" jdbcType="TINYINT"/>
        <result column="createtime" property="createtime" jdbcType="TIMESTAMP"/>
        <result column="updatetime" property="updatetime" jdbcType="TIMESTAMP"/>
    </resultMap>

    <sql id="allColumn">
     id,
     role_name,
     role_desc,
     status,
     createtime,
     updatetime
    </sql>


    <select id="queryRoleInfoAll" resultMap="RoleInfoMap">
        SELECT
        <include refid="allColumn"/>
        FROM user_role
        AND status = 1
    </select>

<!--    <select id="queryUserPage" resultMap="RoleInfoMap">-->
<!--        SELECT-->
<!--        <include refid="allColumn"/>-->
<!--        FROM user_info-->
<!--        <where>-->
<!--            <if test=" query.username != null and query.username != ''">-->
<!--                AND username =#{query.username}-->
<!--            </if>-->
<!--        </where>-->
<!--        ORDER BY createtime DESC-->
<!--        LIMIT #{pageNo} , #{pageSize}-->
<!--    </select>-->

<!--    <select id="queryUserPageCount" resultMap="RoleInfoMap">-->
<!--        SELECT-->
<!--        count(0)-->
<!--        FROM user_info-->
<!--        <where>-->
<!--            <if test=" query.username != null and query.username != ''">-->
<!--                AND username =#{query.username}-->
<!--            </if>-->
<!--        </where>-->
<!--    </select>-->


</mapper>
